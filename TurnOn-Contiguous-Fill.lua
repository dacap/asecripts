-- Turn on the Contiguous option
app.preferences.tool(app.activeTool).contiguous = true

-- Switch the Contiguous option:
--
--   app.command.ContiguousFill()
--
-- or:
--
--   app.preferences.tool(app.activeTool).contiguous =
--     not app.preferences.tool(app.activeTool).contiguous
